import { AppRegistry } from 'react-native';
import App from './App';

AppRegistry.registerComponent('secure-comms-app', () => App);

// This is used by Expo
import { registerRootComponent } from 'expo';
registerRootComponent(App);
